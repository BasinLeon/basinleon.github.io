{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Skill Output Quality Rubric",
  "type": "object",
  "required": [
    "trigger_clarity",
    "actionability",
    "safety_hygiene",
    "portability",
    "overall_pass",
    "notes"
  ],
  "properties": {
    "trigger_clarity": {
      "type": "integer",
      "minimum": 0,
      "maximum": 5,
      "description": "How clearly the skill states when it should be used."
    },
    "actionability": {
      "type": "integer",
      "minimum": 0,
      "maximum": 5,
      "description": "Whether the skill provides deterministic executable guidance."
    },
    "safety_hygiene": {
      "type": "integer",
      "minimum": 0,
      "maximum": 5,
      "description": "Whether secrets, shell usage, and trust boundaries are handled safely."
    },
    "portability": {
      "type": "integer",
      "minimum": 0,
      "maximum": 5,
      "description": "How well the skill can operate across agent runtimes."
    },
    "overall_pass": {
      "type": "boolean"
    },
    "notes": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 1
    }
  }
}
